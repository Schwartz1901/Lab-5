#include<iostream>
#include<iomanip>
#include<stdlib.h>
using namespace std;
int main() {
	int n;
	float x;
	cin >> n >> x;
	float a = 1;
	float b = 1;
	for (int i = 1; i <= n; i++) {
		a = a * x;
		b = b / x;
	}
	cout << setprecision(2) << fixed;
	cout << a << " " << b << endl;
	
}
